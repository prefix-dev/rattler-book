[package]
name        = "mooncolor"
version     = "0.2.0"
build_number = 0
license     = "MIT"
description = "ANSI terminal color and style library — NO_COLOR aware"

[requirements]
run = ["lua >=5.4"]

[build]
script = "build.lua"
noarch = true
