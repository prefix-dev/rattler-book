[package]
name        = "moontemplate"
version     = "0.1.0"
build_number = 0
license     = "MIT"
description = "Mustache-style template engine — sections, partials, escaping"

[requirements]
run = ["lua >=5.4"]

[build]
script = "build.lua"
noarch = true
